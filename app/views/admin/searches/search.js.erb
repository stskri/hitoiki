<% if @model == "user" %>

  $("#admin_search_users").append('<%= j render 'admin/users/users_ajax', users: @records %>');
  <% if @records.next_page %>
    $("#admin_search_more_users_btn").html('<%= j link_to_next_page @records, "さらに読み込む", class: "btn btn-info btn-sm", remote: true %>');
  <% else %>
    $("#admin_search_more_users_btn").remove();
    $("#admin_search_users").append('<div class="my-4 d-flex justify-content-center text-muted" style="font-size: 12px;">最下部です</div>')
  <% end %>

<% elsif @model == "post" %>

  $("#admin_search_posts").append('<%= j render 'admin/posts/posts_ajax', posts: @records %>');
  <% if @records.next_page %>
    $("#admin_search_more_posts_btn").html('<%= j link_to_next_page @records, "さらに読み込む", class: "btn btn-info btn-sm", remote: true %>');
  <% else %>
    $("#admin_search_more_posts_btn").remove();
    $("#admin_search_posts").append('<div class="my-4 d-flex justify-content-center text-muted" style="font-size: 12px;">最下部です</div>')
  <% end %>
<% end %>