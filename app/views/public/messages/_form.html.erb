<%= form_with model: [room, message], class: "d-flex", local: false do |f| %>
  <%= f.text_field :message, placeholder: "メッセージ", class: 'form-control' %>
  <%= f.submit '投稿', class: 'btn btn-primary ml-4' %>
<% end %>