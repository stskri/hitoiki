<!DOCTYPE html>
<html>
  <head>
    <title>Hitoiki</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/97d6fa8903.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container-fluid mx-0">
      <div class="row">
        <% flash.each do |key, message| %>
          <div class="alert alert-<%= key == 'notice' ? 'success' : 'danger' %>">
            <%= message %>
          </div>
        <% end %>
      </div>
      <div class="row">
        <div class="col-2" style="background-color: #eaf4ff;">
          <h3>レフトバー</h3>
        </div>

        <div class="col-7" style="background-color: #f4ffff;"><%= yield %></div>

        <div class="col-3" style="background-color: #eaf4ff;">
          <h3>ライトバー</h3>
        </div>
      </div>
    </div>
  </body>
</html>
